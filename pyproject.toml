[build-system]
requires = ["setuptools>=61.0", "numpy"]
build-backend = "setuptools.build_meta"

[project]
name = "pyexshalos"
version = "0.1.0"
dependencies = [
    "numpy",
    "scipy",
]

[tool.setuptools.packages]
find = {}

